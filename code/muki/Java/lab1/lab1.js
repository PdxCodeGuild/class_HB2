// Average Numbers using Java 

let digits = [0]

let addend = prompt("Please enter an integer or 'q' to quit:\t")

// if (addend = "q") {
//     break
// }

while (addend != q) {
    let addend = parseInt(addend)
    digits.push(addend)
    alert(digits)
}

if (addend = "q") {
    break
}